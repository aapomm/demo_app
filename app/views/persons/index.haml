%main
  %p.reminder
    Upang ipagbigay alam ang pagkawala ng inyong mahal sa buhay,
    maaari niyong I-text ang&nbsp;
    %br
    %code &lt;PANGALAN/EDAD/KASARIAN/HULING LOKASYON&gt;
    %span at ipadala sa
    %code 292901818
  = form_tag '/persons/search', :method => :get, :class => 'search_form' do

    = text_field_tag 'query', nil, {:class => 'js-autocomplete', :data => {'autocomplete-limit' => '10', 'autocomplete' => '/persons/autocomplete'}}
    = submit_tag 'Search'

  = render :partial => 'table', :locals => {:persons => @persons}
